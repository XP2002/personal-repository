#include <stdio.h>

int main(){

    int a, b, c, result

    printf("enter 3 integers");
    scanf("%d %d %d", &a, &b, &c);

    result = a - b - c

    printf("%d - %d - %d\n", a, b, c, result);
    return 0;
}